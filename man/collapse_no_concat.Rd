% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collapse_no_concat.R
\name{collapse_no_concat}
\alias{collapse_no_concat}
\title{Collapse data and keep only key groups where no concatenation is needed}
\usage{
collapse_no_concat(
  .data,
  ...,
  .keys = NULL,
  .concat = NULL,
  sep = " ; ",
  na_rm = TRUE,
  warn = TRUE
)
}
\arguments{
\item{.data}{A data frame or tibble.}

\item{...}{Key columns defining groups (tidyeval).}

\item{.keys}{Optional alternative to …for programmatic key selection. Accepts either (i) a character vector of column names or (ii) a tidyselect expression evaluated in.data. If supplied, .keystakes precedence over…}

\item{.concat}{Optional tidyselect specification of columns used to assess whether concatenation
would be needed. If \code{NULL}, no exclusion is performed.}

\item{sep}{String used to separate concatenated values (passed to \code{collapse_by_keys()}; only relevant
if \code{.concat} is not \code{NULL} and a group is collapsed).}

\item{na_rm}{Logical. If \code{TRUE}, ignore \code{NA} values for distinctness checks and concatenation.}

\item{warn}{Logical. Passed to \code{collapse_by_keys()}. If \code{TRUE}, warns about non-\code{.concat} divergent
columns replaced by \code{NA}.}
}
\value{
A tibble containing one row per key group, restricted to groups where no \code{.concat} column
would require concatenation.
}
\description{
\code{collapse_no_concat()} returns a collapsed table (one row per key combination,
as in \code{collapse_by_keys()}), but restricted to key groups where \emph{no} concatenation
would be needed for columns selected by \code{.concat}.

"No concatenation is needed" means: within a key group, every \code{.concat} column has at most
one distinct value (respecting \code{na_rm}).

If \code{.concat} is \code{NULL}, the function returns the same result as \code{collapse_by_keys()} with
\code{.concat = NULL} (i.e., no groups are excluded on the basis of concatenation).
}
\examples{
library(dplyr)
library(tibble)

df <- tibble(
  exam.num_collec  = c(1, 1, 1, 2, 2),
  mat.matrice      = c("SER", "SER", "SER", "PLAS", "PLAS"),
  spe.denomination = c("E. coli", "E. coli", "E. coli", "S. aureus", "S. aureus"),
  commentaire      = c("first", NA, "repeat", "ok", "ok"),
  source_info      = c("labA", "labA", "labB", "labC", NA),
  value            = c(10, 10, 12, 5, 5)
)

# Keep only collapsed groups where commentaire/source_info do NOT require concatenation
ok <- df \%>\%
  collapse_no_concat(
    exam.num_collec, mat.matrice, spe.denomination,
    .concat = c(commentaire, source_info),
    na_rm = TRUE
  )

ok

}
