% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keep_number_or_na.R
\name{keep_number_or_na}
\alias{keep_number_or_na}
\title{Keep a single number from a string, otherwise return \code{NA}}
\usage{
keep_number_or_na(x)
}
\arguments{
\item{x}{A character vector.}
}
\value{
A character vector of the same length as \code{x}.
}
\description{
\code{keep_number_or_na()} extracts digits from each element of a character vector
according to the following rules:
\itemize{
\item If the string contains \strong{exactly one} number (one contiguous run of digits),
that number is returned (as character).
This includes strings made only of digits (e.g. \code{"15"}) and strings where
the number is mixed with letters or other characters (e.g. \code{"ID=15"}).
\item If the string contains \strong{no} number, the result is \code{NA}.
\item If the string contains \strong{more than one} number (e.g. \code{"15;101"},
\code{"abc12def34"}), the result is \code{NA}.
}

The function is vectorised and can be used inside \code{dplyr::mutate()}.
}
\examples{
x <- c("15", "ID=15", "ser", NA, "15;101", "abc12def34")
keep_number_or_na(x)

df <- tibble::tibble(x = x)
dplyr::mutate(df, y = keep_number_or_na(x))

}
