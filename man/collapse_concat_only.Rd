% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collapse_concat_only.R
\name{collapse_concat_only}
\alias{collapse_concat_only}
\title{Collapse data and keep only key groups where concatenation occurs}
\usage{
collapse_concat_only(
  .data,
  ...,
  .keys = NULL,
  .concat = NULL,
  sep = " ; ",
  na_rm = TRUE,
  warn = TRUE
)
}
\arguments{
\item{.data}{A data frame or tibble.}

\item{...}{Key columns defining groups (tidyeval).}

\item{.keys}{Optional alternative to …for programmatic key selection. Accepts either (i) a character vector of column names or (ii) a tidyselect expression evaluated in.data. If supplied, .keystakes precedence over…}

\item{.concat}{A tidyselect specification of columns that may be concatenated. Must not be \code{NULL}.}

\item{sep}{String used to separate concatenated values.}

\item{na_rm}{Logical. If \code{TRUE}, ignore \code{NA} values for distinctness checks and concatenation.}

\item{warn}{Logical. Passed to \code{collapse_by_keys()}. If \code{TRUE}, warns about non-\code{.concat} divergent
columns replaced by \code{NA}.}
}
\value{
A tibble containing one row per key group, restricted to groups where at least one \code{.concat}
column required concatenation.
}
\description{
\code{collapse_concat_only()} returns a collapsed table (one row per key combination,
as in \code{collapse_by_keys()}), but restricted to key groups where concatenation is
actually needed for at least one column selected by \code{.concat}.

"Concatenation is needed" means: within a key group, at least one \code{.concat} column
has more than one distinct value (respecting \code{na_rm}).
}
\examples{
library(dplyr)
library(tibble)

df <- tibble(
  exam.num_collec  = c(1, 1, 1, 2, 2),
  mat.matrice      = c("SER", "SER", "SER", "PLAS", "PLAS"),
  spe.denomination = c("E. coli", "E. coli", "E. coli", "S. aureus", "S. aureus"),
  commentaire      = c("first", NA, "repeat", "ok", "ok"),
  source_info      = c("labA", "labA", "labB", "labC", NA),
  value            = c(10, 10, 12, 5, 5)
)

# Keep only the collapsed rows where commentaire/source_info needed concatenation
out <- df \%>\%
  collapse_concat_only(
    exam.num_collec, mat.matrice, spe.denomination,
    .concat = c(commentaire, source_info),
    sep = " | ",
    na_rm = TRUE
  )

out

}
