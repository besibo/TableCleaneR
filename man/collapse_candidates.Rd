% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collapse_candidates.R
\name{collapse_candidates}
\alias{collapse_candidates}
\title{Extract original rows from key groups where concatenation would be required}
\usage{
collapse_candidates(.data, ..., .keys = NULL, .concat = NULL, na_rm = TRUE)
}
\arguments{
\item{.data}{A data frame or tibble.}

\item{...}{Key columns defining groups (tidyeval).}

\item{.keys}{Optional alternative to …for programmatic key selection. Accepts either (i) a character vector of column names or (ii) a tidyselect expression evaluated in.data. If supplied, .keystakes precedence over…}

\item{.concat}{A tidyselect specification of columns to check for divergence (and hence
potential concatenation). Must not be \code{NULL}.}

\item{na_rm}{Logical. If \code{TRUE}, ignore \code{NA} values when assessing distinctness within a group.
If \code{FALSE}, \code{NA} participates in distinctness.}
}
\value{
A tibble containing only the input rows that belong to key groups where at least one
\code{.concat} column is divergent (i.e., concatenation would be needed).
}
\description{
\code{collapse_candidates()} identifies key groups (defined by \code{...}) where at least one
column selected by \code{.concat} contains more than one distinct value within the group
(respecting \code{na_rm}). It returns the \emph{original input rows} belonging to those groups.

This is useful for auditing or reviewing duplicates before applying \code{collapse_by_keys()}.
}
\examples{
library(dplyr)
library(tibble)

df <- tibble(
  exam.num_collec  = c(1, 1, 1, 2, 2),
  mat.matrice      = c("SER", "SER", "SER", "PLAS", "PLAS"),
  spe.denomination = c("E. coli", "E. coli", "E. coli", "S. aureus", "S. aureus"),
  commentaire      = c("first", NA, "repeat", "ok", "ok"),
  source_info      = c("labA", "labA", "labB", "labC", NA)
)

# Return the original rows from groups where commentaire and/or source_info would be concatenated
candidates <- df \%>\%
  collapse_candidates(
    exam.num_collec, mat.matrice, spe.denomination,
    .concat = c(commentaire, source_info),
    na_rm = TRUE
  )

candidates

candidates2 <- df \%>\%
  collapse_candidates(
    .keys = c("exam.num_collec", "mat.matrice", "spe.denomination"),
    .concat = c(commentaire, source_info),
    na_rm = TRUE
  )

candidates2

identical(candidates, candidates2)

candidates3 <- df \%>\%
  collapse_candidates(
    .keys = matches("^exam\\\\."),
    .concat = c(commentaire, source_info),
    na_rm = TRUE
  )

candidates3


}
