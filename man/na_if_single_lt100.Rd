% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/na_if_single_lt100.R
\name{na_if_single_lt100}
\alias{na_if_single_lt100}
\title{Replace single integers strictly below 100 with missing values}
\usage{
na_if_single_lt100(x)
}
\arguments{
\item{x}{A character vector.}
}
\value{
A character vector of the same length as \code{x}.
}
\description{
\code{na_if_single_lt100()} scans a character vector and replaces values that are
\strong{exactly one integer} (digits only, no other characters) and \strong{strictly lower
than 100} with \code{NA}.

All other values are returned unchanged, including:
\itemize{
\item \code{NA} values,
\item integers \verb{>= 100},
\item strings that contain multiple numbers (e.g. \code{"15;101"}),
\item strings containing non-digit characters (e.g. \code{"ser"}).
}

The function is vectorised and intended for use inside \code{dplyr::mutate()}.
}
\examples{
x <- c(NA, "15", "99", "100", "015", "15;101", "ser", "101")
na_if_single_lt100(x)

df <- tibble::tibble(x = x)
dplyr::mutate(df, y = na_if_single_lt100(x))

}
