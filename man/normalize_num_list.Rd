% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize_num_list.R
\name{normalize_num_list}
\alias{normalize_num_list}
\title{Normalize a number list stored as a character string}
\usage{
normalize_num_list(x, sep = ";", unique = TRUE)
}
\arguments{
\item{x}{A character vector.}

\item{sep}{Separator used to join multiple integers after normalization.
Default is \code{";"}.}

\item{unique}{Logical. If \code{TRUE}, duplicated integers are removed while preserving
first occurrence order. Default is \code{TRUE}.}
}
\value{
A character vector of the same length as \code{x}.
}
\description{
\code{normalize_num_list()} standardizes strings that contain one or more integers.

Input is accepted only if it contains \strong{digits} and optional \strong{authorized separators}:
spaces, \verb{;}, \code{-}, \verb{,}, and the word \code{"ET"} (case-insensitive, as a token).
If any other character is present (e.g. letters like \code{"ser"}, punctuation like \code{"="}),
the function returns \code{NA}.

Cases:
\itemize{
\item \code{NA} -> \code{NA}.
\item A single integer (e.g. \code{"15"}) -> unchanged.
\item A semicolon-separated list with no spaces (e.g. \code{"15;101;100"}) -> unchanged.
\item Otherwise, integers are extracted and rejoined with \code{sep}.
If no integer is found, returns \code{NA}.
}
}
\examples{
normalize_num_list(c(NA, "15", "15;101", "15 ET 101", "15-100-101", "ser"))

}
